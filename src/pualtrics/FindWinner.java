package pualtrics;

/**
 * Given a number N which represent the number of players participating in
 * Badminton match. The task is to determine the Number of matches required to
 * determine the Winner. In each Match 1 player is knocked out.
 * 
 * Examples:
 * 
 * Input: N = 4 Output: Matches played = 3 (As after each match only N - 1
 * players left)
 * 
 * Input: N = 9 Output: Matches played = 8
 */
public class FindWinner {

	public int noOfMatches(int N) {
		return 0;
	}
}
